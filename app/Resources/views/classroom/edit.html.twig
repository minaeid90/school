{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="col-6">
            <h2>Edit Classroom</h2>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>


{% endblock %}